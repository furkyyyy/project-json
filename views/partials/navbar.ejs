<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Movie Overview</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .nav-link.active {
            background-color: rgba(255, 255, 255, 0.2);
            border-radius: 0.25rem;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">Furky's MovieApp</a>

            <div class="mx-auto">
                <ul class="navbar-nav flex-row">
                    <li class="nav-item me-3">
                        <a class="nav-link <%= (path === "/") ? "active" : "" %>" href="/">Home</a>
                    </li>
                    <li class="nav-item me-3">
                        <a class="nav-link <%= (path === "/movies") ? "active" : "" %>" href="/movies">Movies</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (path === "/studios") ? "active" : "" %>" href="/studios">Studios</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
